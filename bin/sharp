#!/usr/bin/env ruby

case ARGV[0]
when "console"
  require File.expand_path('app/boot', Dir.pwd)
  require 'irb'
  ARGV.clear
  IRB.start(Sharp.root)
when "new"
  Sharp::Application.generate(ARGV[0])
else
  $stderr.puts "Unknown command"
  exit 1
end
